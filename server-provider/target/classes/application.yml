server:
  port: 8081
spring:
  application:
    name: nacos-provider
  cloud:
    nacos:
      discovery:
        server-addr: 10.168.1.10:8848
management:
  endpoints:
    web:
      exposure:
        include: '*'
